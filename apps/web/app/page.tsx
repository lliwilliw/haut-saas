'use client';import {useEffect} from 'react';import {useRouter} from 'next/navigation';import {getToken} from '../lib/api';export default function Home(){const r=useRouter();useEffect(()=>{r.replace(getToken()?'/dashboard':'/login')},[r]);return <div className="center-page">Carregando...</div>}
