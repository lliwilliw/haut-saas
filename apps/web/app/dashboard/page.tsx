'use client';import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import AppShell from '../../components/AppShell';import {api} from '../../lib/api';
export default function Dashboard(){const r=useRouter(),[me,setMe]=useState<any>(null),[contacts,setContacts]=useState<any[]>([]),[ints,setInts]=useState<any>({});useEffect(()=>{Promise.all([api('/auth/me'),api('/contacts?take=5'),api('/integrations/status')]).then(([a,b,c])=>{setMe(a);setContacts(b);setInts(c)}).catch(()=>r.push('/login'))},[r]);const online=Object.values(ints).filter((x:any)=>x?.reachable).length;return <AppShell title="Dashboard"><div className="muted">{me?`${me.organization.name} · ${me.user.name}`:'Carregando...'}</div><div className="grid" style={{marginTop:18}}><div className="card"><div className="muted">Contatos recentes</div><div className="stat">{contacts.length}</div></div><div className="card"><div className="muted">Integrações alcançáveis</div><div className="stat">{online}/4</div></div><div className="card"><div className="muted">Plataforma</div><div className="stat">Online</div></div></div><div className="card" style={{marginTop:18}}><h3>Núcleo inicial</h3><p className="muted">Autenticação, organização, contatos, CRM, integrações e worker separados. O próximo módulo é mensagens e sincronização.</p></div></AppShell>}
