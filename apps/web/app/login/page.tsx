'use client';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';import {setToken} from '../../lib/api';
export default function Login(){const r=useRouter(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setError('');const x=await fetch('/api/proxy/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})}),b=await x.json();if(!x.ok){setError(b.message||'Não foi possível entrar');return}setToken(b.accessToken);r.push('/dashboard')}return <main className="center-page"><form className="auth-card" onSubmit={submit}><h1>Entrar</h1><div className="muted">Acesse sua operação.</div><label className="field"><span>E-mail</span><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="field"><span>Senha</span><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="error">{error}</div>}<button className="btn full">Entrar</button><p className="muted">Primeiro acesso? <Link href="/register">Criar conta</Link></p></form></main>}
