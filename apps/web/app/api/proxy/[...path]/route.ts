import { NextRequest } from 'next/server';
async function proxy(req:NextRequest,ctx:{params:Promise<{path:string[]}>}){const {path}=await ctx.params;const base=(process.env.API_URL||'http://localhost:3001').replace(/\/+$/,'');const target=`${base}/${path.join('/')}${req.nextUrl.search}`;const h=new Headers();for(const k of ['authorization','content-type']){const v=req.headers.get(k);if(v)h.set(k,v)}const body=['GET','HEAD'].includes(req.method)?undefined:await req.text();try{const r=await fetch(target,{method:req.method,headers:h,body,cache:'no-store'});return new Response(await r.text(),{status:r.status,headers:{'content-type':r.headers.get('content-type')||'application/json'}})}catch{return Response.json({message:'Backend indisponível'},{status:502})}}
export const GET=proxy;export const POST=proxy;export const PUT=proxy;export const PATCH=proxy;export const DELETE=proxy;
